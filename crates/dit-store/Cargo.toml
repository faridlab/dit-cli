[package]
name    = "dit-store"
version.workspace      = true
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
dit-model = { workspace = true }
dit-parse = { workspace = true }
thiserror = { workspace = true }
time      = { workspace = true }
# The only entropy source in the workspace: minting ULIDs. Chosen over the
# `ulid`/`rand` stack because it builds everywhere including wasm targets
# without pulling a trait-system dependency graph into the pure core.
getrandom = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
