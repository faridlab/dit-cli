## Expectation

<!-- What the caller believed the contract was: the endpoint/route/field and
     the shape promised — by a doc, a schema, a comment, or the issue this
     work came from. Name where the expectation came from, not just what it
     was. -->

## Actual

<!-- What came back instead, observed first-hand. One or two sentences; the
     evidence blocks below carry the bytes. -->

## Request

<!-- The exact request that produced it: method, path, headers that matter,
     and the full payload. Copy-pasteable — a reader must be able to replay
     it without asking you anything. Redact secrets, and say that you did. -->

```
```

## Response

<!-- The exact response that came back: status, headers that matter, and the
     full body. Verbatim, not summarized — "it returned an error" is not
     evidence. -->

```
```

## Diagnosis

<!-- What you checked before posting this: where the mismatch most plausibly
     originates, with path:line pointers if you looked. If you could not
     investigate from your side, say so plainly — that is the first thing to
     establish together. -->

## Unblock

<!-- What answer or change closes this report: a fix on the owner's side, a
     correction of the expectation on yours, or a decision to file it
     separately. Name the issue it should be tracked in, if it is not this
     one. -->
