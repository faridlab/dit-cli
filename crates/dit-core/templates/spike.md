## Question

<!-- What must this spike answer before time runs out? One numbered list;
     each question answerable with evidence, not opinion. If the whole spike
     is one decision, phrase it as "A or B, and why". Mark which question is
     load-bearing — the one that decides the outcome regardless of the
     others. -->

1.

## What the codebase holds

<!-- Evidence gathered before investigating: the adjacent concepts that
     exist, with path:line pointers, and a plain statement of what does NOT
     exist. A table | Thing | Where | What it is | works well. This section
     separates "we looked and it isn't there" from "we didn't look". -->

## Output

<!-- The decision artifact this spike produces, as one of a small set of
     named outcomes ("drop the feature / ship it as an upload-and-list /
     write implementation tickets for a generator"). The spike is done when
     one outcome is recommended with evidence, not when time runs out. -->

## Timebox

<!-- The budget and what gets cut first when it is hit. -->

## Do not

<!-- What the spike must not do: no implementation, no migrations, nothing
     a downstream ticket would have to redo. The point is to decide, not to
     build. -->

## Findings

<!-- Filled in at the end: answers to each numbered question, with the
     evidence that settles them. If an answer corrects the question's
     premise, say so plainly. -->

## Recommendation

<!-- Filled in at the end: the chosen Output outcome, bolded, with the one
     property that decided it. -->
