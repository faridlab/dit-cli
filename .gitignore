/target
**/*.rs.bk
Cargo.lock.bak

# DIT's own index — disposable by design (Principle 2), never committed.
.dit-cache/
.dit-worktree/

node_modules/
apps/web/dist/
# TypeScript's incremental build cache — pure derived state.
*.tsbuildinfo
.env
.env.*
!.env.example

.DS_Store
*.log

.claude/

# The UI bundle is a build artifact, never committed (DESIGN.md §8).
apps/web/dist/

# The editor's WASM bridge — wasm-pack output (`just wasm-build`), same
# category as dist/: derived from crates/dit-wasm, never committed.
apps/web/src/editor/wasm/